#include<bits/stdc++.h>
using namespace std;

//recursive function for calculating the factorial.

int fact(int n)
{
    if(n==1)
    {
        return 1;
    }
    return fact(n-1) * n;
}


int main()
{
    int n;
    cin>>n;
    cout<<fact(n);
    return 0;
}
